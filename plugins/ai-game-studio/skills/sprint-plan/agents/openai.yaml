interface:
  display_name: "Sprint Plan"
  short_description: "Generates a new sprint plan or updates an existing one based"
  default_prompt: "Use $ai-game-studio:sprint-plan to generates a new sprint plan or updates an existing one based for this project and report evidence."
policy:
  allow_implicit_invocation: true
