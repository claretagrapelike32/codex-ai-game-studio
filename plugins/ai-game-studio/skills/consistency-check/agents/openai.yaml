interface:
  display_name: "Consistency Check"
  short_description: "Scan all GDDs against the entity registry to detect"
  default_prompt: "Use $ai-game-studio:consistency-check to scan all GDDs against the entity registry to detect for this project and report evidence."
policy:
  allow_implicit_invocation: true
