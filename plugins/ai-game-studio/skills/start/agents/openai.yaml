interface:
  display_name: "Start"
  short_description: "First-time onboarding — asks where you are, then guides you"
  default_prompt: "Use $ai-game-studio:start to inspect this project and route me to the safest next game-development workflow."
policy:
  allow_implicit_invocation: true
