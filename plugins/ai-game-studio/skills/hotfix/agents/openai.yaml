interface:
  display_name: "Hotfix"
  short_description: "Emergency fix workflow that bypasses normal sprint processes"
  default_prompt: "Use $ai-game-studio:hotfix to emergency fix workflow that bypasses normal sprint processes for this project and report evidence."
policy:
  allow_implicit_invocation: true
