interface:
  display_name: "Toolchain Doctor"
  short_description: "Inspect the local game toolchain safely"
  default_prompt: "Use $ai-game-studio:toolchain-doctor to inspect this project's OS, engine, DCC tools, GPU, and MCP setup without changing anything."
policy:
  allow_implicit_invocation: true
