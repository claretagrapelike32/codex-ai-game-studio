interface:
  display_name: "Reverse Document"
  short_description: "Generate design or architecture documents from existing"
  default_prompt: "Use $ai-game-studio:reverse-document to generate design or architecture documents from existing for this project and report evidence."
policy:
  allow_implicit_invocation: true
