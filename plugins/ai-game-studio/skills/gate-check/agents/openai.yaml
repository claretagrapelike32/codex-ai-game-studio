interface:
  display_name: "Gate Check"
  short_description: "Validate readiness to advance between development phases."
  default_prompt: "Use $ai-game-studio:gate-check to validate readiness to advance between development phases. for this project and report evidence."
policy:
  allow_implicit_invocation: true
