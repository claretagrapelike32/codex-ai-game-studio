interface:
  display_name: "Test Helpers"
  short_description: "Generate engine-specific test helper libraries for the"
  default_prompt: "Use $ai-game-studio:test-helpers to generate engine-specific test helper libraries for the for this project and report evidence."
policy:
  allow_implicit_invocation: true
