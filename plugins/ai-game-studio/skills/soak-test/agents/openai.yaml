interface:
  display_name: "Soak Test"
  short_description: "Generate a soak test protocol for extended play sessions."
  default_prompt: "Use $ai-game-studio:soak-test to generate a soak test protocol for extended play sessions. for this project and report evidence."
policy:
  allow_implicit_invocation: true
