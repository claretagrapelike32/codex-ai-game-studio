interface:
  display_name: "Project Stage Detect"
  short_description: "Automatically analyze project state, detect stage, identify"
  default_prompt: "Use $ai-game-studio:project-stage-detect to automatically analyze project state, detect stage, identify for this project and report evidence."
policy:
  allow_implicit_invocation: true
