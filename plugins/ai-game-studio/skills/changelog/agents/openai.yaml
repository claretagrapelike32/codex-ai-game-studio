interface:
  display_name: "Changelog"
  short_description: "Auto-generates a changelog from git commits, sprint data,"
  default_prompt: "Use $ai-game-studio:changelog to auto-generates a changelog from git commits, sprint data, for this project and report evidence."
policy:
  allow_implicit_invocation: true
