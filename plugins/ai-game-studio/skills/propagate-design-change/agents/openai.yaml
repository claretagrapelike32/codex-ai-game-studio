interface:
  display_name: "Propagate Design Change"
  short_description: "When a GDD is revised, scans all ADRs and the traceability"
  default_prompt: "Use $ai-game-studio:propagate-design-change to when a GDD is revised, scans all ADRs and the traceability for this project and report evidence."
policy:
  allow_implicit_invocation: true
