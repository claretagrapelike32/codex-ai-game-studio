interface:
  display_name: "Onboard"
  short_description: "Generates a contextual onboarding document for a new"
  default_prompt: "Use $ai-game-studio:onboard to generates a contextual onboarding document for a new for this project and report evidence."
policy:
  allow_implicit_invocation: true
