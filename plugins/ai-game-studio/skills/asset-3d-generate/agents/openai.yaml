interface:
  display_name: "Asset 3D Generate"
  short_description: "Generate and validate engine-ready 3D assets"
  default_prompt: "Use $ai-game-studio:asset-3d-generate to choose and validate a licensed engine-ready 3D generation pipeline."
policy:
  allow_implicit_invocation: true
