interface:
  display_name: "Architecture Decision"
  short_description: "Creates an Architecture Decision Record (ADR) documenting a"
  default_prompt: "Use $ai-game-studio:architecture-decision to creates an Architecture Decision Record (ADR) documenting a for this project and report evidence."
policy:
  allow_implicit_invocation: true
