interface:
  display_name: "Playtest Report"
  short_description: "Generates a structured playtest report template or analyzes"
  default_prompt: "Use $ai-game-studio:playtest-report to generates a structured playtest report template or analyzes for this project and report evidence."
policy:
  allow_implicit_invocation: true
